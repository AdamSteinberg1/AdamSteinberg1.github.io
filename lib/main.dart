import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
      title: 'Adam Steinberg',
      home: Scaffold(
          appBar: AppBar(title: const Text("Adam Steinberg")),
          body: const Center(child: Text("Hello World!")))));
}
